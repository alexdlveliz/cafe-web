<div class="album text-muted">
  <div class="container">
    <div class="row <%= "sortable" if logged_in?(:site_admin) %>">
        <%# Recorrer cada evento y mostrarlo en pantalla %>
        <%= render partial: 'events_item', collection:  @events_items %>
    </div>

    <%= link_to 'Crear Nuevo evento', new_event_path(@events_item) if logged_in?(:site_admin) %>
  </div>
</div>