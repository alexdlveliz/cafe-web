<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://fonts.googleapis.com/css?family=Josefin+Slab|Montez&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Josefin+Slab:400,700&display=swap" rel="stylesheet">
    <%# meta para poner palabras claves %>
    <meta name="keywords" content="<%= @seo_keywords %>" />

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="site-wrapper">
      <div class="site-wrapper-inner">
        <div class="cover-container">
          <%= image_tag "fcafe.jpg", id: "background"%>
          <%# Llamando al partial llamado application_nav %>
          <%= render "shared/application_nav" %>
          
          <%# Permite conectar con cualquier otro archivo html%>
          <%= yield %>
          <%# Llamando al partial llamado application_footer %>
          <%= render "shared/application_footer" %>
          <%# Llamando a un método helper para las alertas %>
          <%= alerts %>
        </div>
      </div>
    </div>
  </body>
</html>
