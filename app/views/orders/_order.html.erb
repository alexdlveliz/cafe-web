<tr>
    <td><%= order.price %></td>
    <td><%= order.quantity %></td>
    <td><%= order.product %></td>
    <td><%= order.sell %></td>
    <td><%= link_to 'Show', order_show_path(order) %></td>
    <td><%= link_to 'Edit', edit_order_path(order) %></td>
    <td><%= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>